
import * as React from "react";
import { IRoutedCompProps } from "../../routes";

export interface IPortfolioProps extends IRoutedCompProps {

};

export const Portfolio: React.SFC<IPortfolioProps> = (props) => {
    return (
        <>
        </>
    );
};